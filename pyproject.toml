[tool.ruff]
line-length = 120
target-version = "py311"
exclude = [
  "myenv",
  "envConfig",
  "env",
  ".venv",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "C4", "RUF"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
